= turbo_frame_tag "#{object_name(objects)}_page" do
  = render objects
  - unless objects.last_page?
    = turbo_frame_tag "#{object_name(objects)}_page", :src => path_to_next_page(objects), :loading => "lazy"
